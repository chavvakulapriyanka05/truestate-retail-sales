# TruEstate Retail Sales Backend

Simple Node.js + Express backend that loads the provided sales dataset from CSV into memory and exposes a single `/api/sales` endpoint supporting search, filters, sorting, and pagination.

## Commands

```bash
npm install
npm run dev   # start with nodemon
# or
npm start
```
